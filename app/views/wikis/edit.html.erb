<h1>Edit Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for Wikis</p>
    <ul>
      <li>Make sure it rhymes.</li>
      <li>Don't use the letter "A".</li>
      <li>The incessant use of hashtags will get you banned.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= render partial: 'form' %>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <h3>Manage collaborators</h3>
      <% @users.each do |user| %>
        <p><%= user.email %></p>
        <% if @wiki.users.include?(user) %>
          <p><%= link_to 'remove collaborator', wiki_collaborators_path(user_id: user.id, wiki_id: @wiki.id), method: :delete, class: 'btn btn-warning' %></p>
        <% else %>
          <p><%= link_to 'add collaborator', wiki_collaborators_path(user_id: user.id, wiki_id: @wiki.id), method: :post, class: 'btn btn-success' %></p>
        <% end %>
      <% end %>
  </div>
</div>
